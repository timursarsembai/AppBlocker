<UserControl x:Class="AppBlocker.UI.Views.DopamineMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Дофаминовое меню" FontSize="32" FontWeight="Bold" Foreground="White" Margin="0,0,0,10"/>
        
        <TextBlock Grid.Row="1" Text="Когда рука тянется к соцсетям, выберите одно действие из списка ниже. Это даст мозгу быструю, но здоровую дозу дофамина." 
                   FontSize="14" Foreground="#A1A1AA" TextWrapping="Wrap" Margin="0,0,0,30"/>

        <!-- Список привычек -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Habits}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#27272A" CornerRadius="8" Padding="15" Margin="0,0,0,10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Чекбокс выполнения -->
                                <CheckBox Grid.Column="0" IsChecked="{Binding IsCompleted}" VerticalAlignment="Center">
                                    <CheckBox.LayoutTransform>
                                        <ScaleTransform ScaleX="1.5" ScaleY="1.5" />
                                    </CheckBox.LayoutTransform>
                                </CheckBox>

                                <!-- Название привычки -->
                                <TextBlock Grid.Column="1" Text="{Binding Title}" FontSize="18" Foreground="White" 
                                           VerticalAlignment="Center" Margin="15,0,0,0">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <!-- Зачеркиваем текст, если выполнено -->
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                                                    <Setter Property="TextDecorations" Value="Strikethrough"/>
                                                    <Setter Property="Foreground" Value="#52525B"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>

                                <!-- Кнопка удаления -->
                                <Button Grid.Column="2" Content="✕" 
                                        Command="{Binding DataContext.RemoveHabitCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"
                                        Background="Transparent" Foreground="#F87171" BorderThickness="0"
                                        FontSize="18" FontWeight="Bold" Cursor="Hand" Padding="10,0"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Добавление новой привычки -->
        <Grid Grid.Row="3" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0" Text="{Binding NewHabitText, UpdateSourceTrigger=PropertyChanged}" 
                     Background="#18181B" Foreground="White" BorderBrush="#3F3F46" BorderThickness="1" 
                     Padding="15,10" FontSize="16" 
                     HorizontalAlignment="Stretch"/>

            <Button Grid.Column="1" Content="Добавить" Command="{Binding AddHabitCommand}"
                    Background="#10B981" Foreground="White" FontSize="16" FontWeight="SemiBold"
                    BorderThickness="0" Padding="20,10" Margin="10,0,0,0">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>
    </Grid>
</UserControl>
