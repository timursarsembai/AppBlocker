<UserControl x:Class="AppBlocker.UI.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">

    <UserControl.Resources>
        <!-- Стилизованная карточка-кнопка с эффектом ScaleTransform при наведении -->
        <Style x:Key="AnimatedCardStyle" TargetType="Button">
            <Setter Property="Background" Value="#18181B"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#27272A"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="16" Padding="15,40">
                            <!-- Эффект мягкой тени для объема -->
                            <Border.Effect>
                                <DropShadowEffect Color="Black" Opacity="0.2" BlurRadius="20" ShadowDepth="10" />
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#27272A"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#3F3F46"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <!-- Анимация увеличения при наведении -->
                <EventTrigger RoutedEvent="MouseEnter">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX" To="1.04" Duration="0:0:0.15" />
                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY" To="1.04" Duration="0:0:0.15" />
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
                <!-- Возврат в исходное состояние -->
                <EventTrigger RoutedEvent="MouseLeave">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX" To="1" Duration="0:0:0.15" />
                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY" To="1" Duration="0:0:0.15" />
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Блок статуса по центру -->
        <StackPanel Grid.Row="0" Margin="0,20,0,30" HorizontalAlignment="Center">
            <!-- Название статуса / режима -->
            <TextBlock Text="{Binding StatusText}" 
                       Foreground="{Binding StatusColor}" 
                       FontSize="20" FontWeight="SemiBold" 
                       HorizontalAlignment="Center" TextAlignment="Center"
                       Margin="0,0,0,5"/>
            <!-- Крупный таймер -->
            <TextBlock Text="{Binding StatusTimer}" 
                       Foreground="{Binding StatusColor}" 
                       FontSize="64" FontWeight="Bold" 
                       HorizontalAlignment="Center" TextAlignment="Center"
                       Margin="0,0,0,15">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Visible"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding StatusTimer}" Value="">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
            <!-- Текст под таймером (скрывается при активной сессии) -->
            <TextBlock Text="Выберите время для начала новой сессии" 
                       Foreground="#A1A1AA" FontSize="18" 
                       HorizontalAlignment="Center">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Visible"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsSessionActive}" Value="True">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </StackPanel>

        <!-- Кнопка Остановить (видна только при активной сессии) -->
        <Button Grid.Row="1" Content="⏹  Остановить сессию" 
                Command="{Binding StopSessionCommand}" 
                HorizontalAlignment="Center" Margin="0,0,0,40"
                Background="#451A1E" Foreground="#F87171" FontSize="16" FontWeight="SemiBold"
                BorderThickness="1" BorderBrush="#7F1D1D" Padding="30,12" Cursor="Hand">
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsSessionActive}" Value="True">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="stopBorder" Background="{TemplateBinding Background}" 
                            BorderBrush="{TemplateBinding BorderBrush}" 
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="10">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="stopBorder" Property="Background" Value="#7F1D1D"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Button.Template>
        </Button>

        <!-- Кастомный таймер и быстрые кнопки (скрываются при активной сессии) -->
        <StackPanel Grid.Row="2" VerticalAlignment="Top" HorizontalAlignment="Center" Margin="0,20,0,0">
            <StackPanel.Style>
                <Style TargetType="StackPanel">
                    <Setter Property="Visibility" Value="Visible"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsSessionActive}" Value="True">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </StackPanel.Style>

            <!-- Ввод времени -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
                <!-- Часы -->
                <StackPanel Margin="0,0,15,0">
                    <TextBlock Text="Часы" Foreground="#A1A1AA" FontSize="14" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Text="{Binding InputHours, UpdateSourceTrigger=PropertyChanged}" 
                                 Width="70" Height="60" FontSize="30" TextAlignment="Center"
                                 Background="#18181B" Foreground="White" BorderBrush="#3F3F46" BorderThickness="1"
                                 VerticalContentAlignment="Center" HorizontalContentAlignment="Center"
                                 PreviewMouseWheel="Hours_PreviewMouseWheel"/>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="4,0,0,0">
                            <RepeatButton Content="▲" FontSize="10" Height="28" Width="24" Click="HoursUp_Click" Background="#27272A" Foreground="#A1A1AA" BorderThickness="0" Margin="0,0,0,4" Cursor="Hand"/>
                            <RepeatButton Content="▼" FontSize="10" Height="28" Width="24" Click="HoursDown_Click" Background="#27272A" Foreground="#A1A1AA" BorderThickness="0" Cursor="Hand"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>

                <!-- Двоеточие -->
                <TextBlock Text=":" Foreground="White" FontSize="40" FontWeight="Bold" 
                           VerticalAlignment="Center" Margin="0,15,15,0"/>

                <!-- Минуты -->
                <StackPanel Margin="0,0,20,0">
                    <TextBlock Text="Минуты" Foreground="#A1A1AA" FontSize="14" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Text="{Binding InputMinutes, UpdateSourceTrigger=PropertyChanged}" 
                                 Width="70" Height="60" FontSize="30" TextAlignment="Center"
                                 Background="#18181B" Foreground="White" BorderBrush="#3F3F46" BorderThickness="1"
                                 VerticalContentAlignment="Center" HorizontalContentAlignment="Center"
                                 PreviewMouseWheel="Minutes_PreviewMouseWheel"/>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="4,0,0,0">
                            <RepeatButton Content="▲" FontSize="10" Height="28" Width="24" Click="MinutesUp_Click" Background="#27272A" Foreground="#A1A1AA" BorderThickness="0" Margin="0,0,0,4" Cursor="Hand"/>
                            <RepeatButton Content="▼" FontSize="10" Height="28" Width="24" Click="MinutesDown_Click" Background="#27272A" Foreground="#A1A1AA" BorderThickness="0" Cursor="Hand"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>

                <!-- Кнопка Старт -->
                <Button Content="▶  Начать" Command="{Binding StartCustomSessionCommand}" 
                        Background="#34D399" Foreground="#064E3B" FontSize="18" FontWeight="Bold"
                        BorderThickness="0" Padding="25,0" Cursor="Hand" Height="60" Margin="0,22,0,0">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="startBorder" Background="{TemplateBinding Background}" CornerRadius="8">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="startBorder" Property="Background" Value="#10B981"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>

            <!-- Быстрые пресеты -->
            <TextBlock Text="или выберите готовое время:" Foreground="#71717A" FontSize="14" HorizontalAlignment="Center" Margin="0,10,0,15"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="120 мин" Command="{Binding StartQuickSessionCommand}" CommandParameter="120"
                        Background="#27272A" Foreground="White" FontSize="16" BorderThickness="0" 
                        Padding="20,10" Margin="0,0,10,0" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="btnBorder" Background="{TemplateBinding Background}" CornerRadius="20">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="btnBorder" Property="Background" Value="#3F3F46"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                
                <Button Content="60 мин" Command="{Binding StartQuickSessionCommand}" CommandParameter="60"
                        Background="#27272A" Foreground="White" FontSize="16" BorderThickness="0" 
                        Padding="20,10" Margin="0,0,10,0" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="btnBorder" Background="{TemplateBinding Background}" CornerRadius="20">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="btnBorder" Property="Background" Value="#3F3F46"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                
                <Button Content="25 мин" Command="{Binding StartQuickSessionCommand}" CommandParameter="25"
                        Background="#27272A" Foreground="White" FontSize="16" BorderThickness="0" 
                        Padding="20,10" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="btnBorder" Background="{TemplateBinding Background}" CornerRadius="20">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="btnBorder" Property="Background" Value="#3F3F46"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>
            
        </StackPanel>
    </Grid>
</UserControl>
